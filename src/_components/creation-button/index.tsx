import { PropsWithChildren } from 'react'
import style from '@/_components/creation-button/layout.module.css'

export async function CreationButton(){
return (
    <button className={style.creationButton}>
        +
    </button>
)
}